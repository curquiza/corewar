version: 2
jobs:
  build:
    docker:
      - image: gcc:8
    #branches:
      #only:
        #- master
        #- asm
    steps:
      - checkout
      - run:
          name: Current Time
          command: date
      - run:
          name: ASM Compilation
          command: make
  test:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run: python3 asm/tests/main.py

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
